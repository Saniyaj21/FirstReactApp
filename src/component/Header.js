import React from "react";

// function component
const Header = (props) => {
    return (
        <div >
            <h2>Contact Mannager</h2>

            <hr />
        </div>
    );
};

export default Header;